#include "../include/tiobj.hpp"

#include <iostream>

using namespace std;

/*
TiStream::TiStream(FILE* fd){
	this->parser.loadFile(fd);
}

bool TiStream::next(TiObj& out){
	out.clear();
	bool res = this->parser.parseStream();
	cout << this->parser.output.text << endl;
	parseTiAsm(out,this->parser.output.text);
	return res;
}*/
